export const BREAKPOINTS = {
  mobile: 768,
  tablet: 1024,
} as const;

export const BOOK_CONFIG = {
  minWidth: 320,
  maxWidth: 1200,
  minHeight: 480,
  maxHeight: 1600,
  flippingTime: 800,
  swipeDistance: 50,
  maxShadowOpacity: 0.5,
} as const;

export const DEFAULT_BOOK_ID = "peter-rabbit";
